#include <stdio.h>
#include "my_mat.h"

int main() {
    char yourchoice;
    scanf(" %c", &yourchoice);

    while (yourchoice != 'D')
    {
        if (yourchoice == 'A')
        {

        }

        else if (yourchoice == 'B')
        {

        }

        else if (yourchoice == 'C')
        {

        }

        else   
            continue;
    }

    return 0;
}